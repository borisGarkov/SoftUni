from django.core.exceptions import ValidationError


def validate_img_extensions(string):
    pass
    # try:
    #     if string.split('profile photos /')
    #
    #     format_str = string.format
    #     if format_str.lower() not in ['png', 'jpg', 'jpeg']:
    #         raise ValidationError('')
    # except Exception:
    #     raise ValidationError('Not valid image extension')

